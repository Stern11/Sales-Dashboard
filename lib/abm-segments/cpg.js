// CPG ABM segment — stub. Not yet populated: drop this vertical's target
// company/lead HubSpot IDs into `leads`/`companies` (same shape as
// logistics.js) and it appears in the dashboard's segment tabs automatically
// — api/segments.js only lists segments with leads.length > 0.

export const id = "cpg";
export const label = "CPG";
export const leads = [];
export const companies = [];
